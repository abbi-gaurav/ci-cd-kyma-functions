{
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "nock": "^13.0.5",
    "nyc": "^15.1.0",
    "redis-mock": "^0.56.3",
    "rewiremock": "^3.14.3"
  },
  "scripts": {
    "test": "mocha",
    "test-with-coverage": "nyc --reporter=lcov --reporter=text mocha --lines 90 --per-file"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "redis": "^3.0.2"
  }
}
